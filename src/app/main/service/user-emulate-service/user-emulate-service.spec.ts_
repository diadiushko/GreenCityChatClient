import { TestBed } from '@angular/core/testing';

import { UserEmulateServiceService } from './user-emulate-service.service';

describe('UserEmulateServiceService', () => {
  let service: UserEmulateServiceService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(UserEmulateServiceService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
